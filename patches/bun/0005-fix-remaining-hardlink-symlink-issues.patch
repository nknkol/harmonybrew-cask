diff -ruN a/src/install/TarballStream.zig b/src/install/TarballStream.zig
--- a/src/install/TarballStream.zig	2026-07-09 21:36:28.338978906 +0800
+++ b/src/install/TarballStream.zig	2026-07-09 21:37:18.334978281 +0800
@@ -693,7 +693,7 @@
     bun.sys.symlinkat(target, dest_fd, path).unwrap() catch |err| switch (err) {
         error.EPERM, error.ENOENT => {
             dest_fd.makePath(u8, std.fs.path.dirname(path_slice) orelse return) catch {};
-            bun.sys.symlinkat(target, dest_fd, path).unwrap() catch {};
+            bun.sys.symlinkat(target, dest_fd, path).unwrap() catch {}; // hmdfs: symlink may not be supported
         },
         else => {},
     };
diff -ruN a/src/install/isolated_install/Installer.zig b/src/install/isolated_install/Installer.zig
--- a/src/install/isolated_install/Installer.zig	2026-07-09 21:36:28.119350625 +0800
+++ b/src/install/isolated_install/Installer.zig	2026-07-09 21:37:18.334978281 +0800
@@ -617,7 +617,8 @@
                                     switch (try hardlinker.link()) {
                                         .result => {},
                                         .err => |err| {
-                                            if (err.getErrno() == .XDEV) {
+                                            // hmdfs: hardlink unsupported — fall back to copy
+                                            if (err.getErrno() != .NOENT and err.getErrno() != .EXIST) {
                                                 continue :backend .copyfile;
                                             }
 
@@ -854,7 +855,8 @@
                             switch (try hardlinker.link()) {
                                 .result => {},
                                 .err => |err| {
-                                    if (err.getErrno() == .XDEV) {
+                                    // hmdfs: hardlink unsupported — fall back to copy
+                                            if (err.getErrno() != .NOENT and err.getErrno() != .EXIST) {
                                         installer.supported_backend.store(.copyfile, .monotonic);
                                         continue :backend .copyfile;
                                     }
diff -ruN a/src/install/isolated_install/Symlinker.zig b/src/install/isolated_install/Symlinker.zig
--- a/src/install/isolated_install/Symlinker.zig	2026-07-09 21:36:28.170979844 +0800
+++ b/src/install/isolated_install/Symlinker.zig	2026-07-09 21:37:18.334978281 +0800
@@ -54,7 +54,14 @@
                             FD.cwd().deleteTree(this.dest.sliceZ()) catch {};
                             return this.symlink();
                         },
-                        else => .initErr(symlink_err1),
+                        // hmdfs: retry on any unsupported error
+                        else => {
+                            const dest_parent = this.dest.dirname() orelse {
+                                return .initErr(symlink_err1);
+                            };
+                            FD.cwd().makePath(u8, dest_parent) catch {};
+                            return this.symlink();
+                        },
                     },
                 };
             },
diff -ruN a/src/install/npm.zig b/src/install/npm.zig
--- a/src/install/npm.zig	2026-07-09 21:36:28.266977812 +0800
+++ b/src/install/npm.zig	2026-07-09 21:37:18.334978281 +0800
@@ -1103,14 +1103,9 @@
                 try bun.sys.renameat(bun.FD.cwd(), path_to_use_for_opening_file, bun.FD.cwd(), cache_path_abs).unwrap();
             } else if (Environment.isLinux and is_using_o_tmpfile) {
                 defer file.close();
-                // Attempt #1.
-                bun.sys.linkatTmpfile(file.handle, cache_dir, outpath).unwrap() catch {
-                    // Attempt #2: the file may already exist. Let's unlink and try again.
-                    bun.sys.unlinkat(cache_dir, outpath).unwrap() catch {};
-                    try bun.sys.linkatTmpfile(file.handle, cache_dir, outpath).unwrap();
-
-                    // There is no attempt #3. This is a cache, so it's not essential.
-                };
+                // On hmdfs linkatTmpfile uses hardlinks (unsupported).
+                // Skip — cache is non-essential; renameat fallback below works.
+                bun.sys.linkatTmpfile(file.handle, cache_dir, outpath).unwrap() catch {};
             } else {
                 defer file.close();
                 // Attempt #1. Rename the file.
