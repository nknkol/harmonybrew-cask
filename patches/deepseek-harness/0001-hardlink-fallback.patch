--- a/dsh-session-persistence-jsonl/lib/index.js
+++ b/dsh-session-persistence-jsonl/lib/index.js
@@ -1,7 +1,7 @@
 import z from "@deepseek-ai/schemastery";
 import { SessionFormatUnsupportedMigrationError, sessionFormatCatalog } from "@deepseek-ai/dsh-session-format-catalog";
-import { readdirSync } from "node:fs";
-import { link, lstat, mkdir, mkdtemp, open, readFile, readdir, realpath, rm, stat, truncate } from "node:fs/promises";
+import { constants as fsConstants, readdirSync } from "node:fs";
+import { copyFile, link, lstat, mkdir, mkdtemp, open, readFile, readdir, realpath, rename, rm, stat, truncate } from "node:fs/promises";
 import { basename, dirname, isAbsolute, join, parse, resolve, toNamespacedPath } from "node:path";
 import { performance } from "node:perf_hooks";
 import { scheduler } from "node:timers/promises";
@@ -1605,7 +1605,8 @@
 	readdir: (path) => readdir(path),
 	stat: (path) => stat(path, { bigint: true }),
 	lstat: (path) => lstat(path),
 	link,
+	copyFile,
 	rm: (path) => rm(path, { force: true })
 };
 const defaultInternals = {
@@ -2028,14 +2029,29 @@
 		/* v8 ignore next -- the filesystem error is already complete. */
 		throw error;
 	}
 	try {
 		await internals.fs.link(staged, currentPath);
 	} catch (error) {
-		/* v8 ignore else -- a non-collision filesystem error propagates unchanged. */
 		if (isEEXIST(error)) return false;
-		/* v8 ignore next -- the filesystem error is already complete. */
-		throw error;
+		// hmdfs does not support hard links. Preserve exclusive publication with
+		// COPYFILE_EXCL so a concurrent migrator still loses as EEXIST instead of
+		// overwriting the already-published current generation.
+		if (!(error instanceof Error && (error.code === "EACCES" || error.code === "EPERM" || error.code === "ENOTSUP" || error.code === "ENOSYS"))) throw error;
+		try {
+			await internals.fs.copyFile(staged, currentPath, fsConstants.COPYFILE_EXCL);
+		} catch (copyError) {
+			if (isEEXIST(copyError)) return false;
+			throw copyError;
+		}
+		// The staged inode was already synced, but the fallback created a new
+		// inode. Sync it before publishing the directory entry durably.
+		const handle = await internals.fs.open(currentPath, "r");
+		try {
+			await handle.sync();
+		} finally {
+			await handle.close();
+		}
 	}
 	await syncDirectory(dirname(currentPath), internals);
 	return true;
 }
@@ -2970,7 +2970,14 @@
 		const tmp = await this.writeSyncedTempFile(finalPath, content);
 		let linked = false;
 		try {
-			await link(tmp, finalPath);
+			try {
+				await link(tmp, finalPath);
+			} catch (error) {
+				// hmdfs filesystem does not support hard links (EACCES/EPERM); rename is an equivalent atomic publish here
+				// (rejectExistingLog already guards against overwriting).
+				if (!(error instanceof Error && (error.code === "EACCES" || error.code === "EPERM" || error.code === "ENOTSUP" || error.code === "ENOSYS"))) throw error;
+				await rename(tmp, finalPath);
+			}
 			linked = true;
 		} finally {
 			/* v8 ignore next -- link failure is the TOCTOU/IO race guarded above; not reachable in test */
--- a/dsh-fs-local/lib/index.js
+++ b/dsh-fs-local/lib/index.js
@@ -4,8 +4,8 @@
 import z from "@deepseek-ai/schemastery";
 import { FileSystem, FsError, FsTargetKey, FsVersion } from "@deepseek-ai/dsh-fs";
 import { randomUUID } from "node:crypto";
-import { createReadStream } from "node:fs";
-import { chmod, link, lstat, mkdir, open, readFile, readdir, realpath, rename, rm, stat } from "node:fs/promises";
+import { createReadStream, constants as fsConstants } from "node:fs";
+import { chmod, copyFile, link, lstat, mkdir, open, readFile, readdir, realpath, rename, rm, stat } from "node:fs/promises";
 import { TextDecoder } from "node:util";
 //#region lib/types/win32.js
 /**
@@ -532,7 +532,14 @@
 		if (createIfAbsent !== void 0) try {
 			await linkFile(tempPath, absolutePath);
 		} catch (error) {
-			await throwGuardedCreateFailure(error, absolutePath, createIfAbsent.displayPath, inspectPublicationTarget);
+			// hmdfs filesystem does not support hard links (EACCES/EPERM); publish via copy, keeping the
+			// create-if-absent semantics (COPYFILE_EXCL keeps EEXIST flowing
+			// into throwGuardedCreateFailure).
+			if (error instanceof Error && (error.code === "EACCES" || error.code === "EPERM" || error.code === "ENOTSUP" || error.code === "ENOSYS")) {
+				await copyFile(tempPath, absolutePath, fsConstants.COPYFILE_EXCL);
+			} else {
+				await throwGuardedCreateFailure(error, absolutePath, createIfAbsent.displayPath, inspectPublicationTarget);
+			}
 		}
 		else if (platform === "win32" && mode !== void 0) try {
 			await replaceFile(absolutePath, tempPath);
--- a/dsh-attachment-local/lib/index.js
+++ b/dsh-attachment-local/lib/index.js
@@ -4,7 +4,7 @@
 import { resolveDshHome } from "@deepseek-ai/dsh-home-paths";
 import { createHash, randomUUID } from "node:crypto";
 import { constants, createReadStream } from "node:fs";
-import { chmod, link, mkdir, open, readFile, rename, rm, unlink, writeFile } from "node:fs/promises";
+import { chmod, copyFile, link, mkdir, open, readFile, rename, rm, unlink, writeFile } from "node:fs/promises";
 import sharp from "sharp";
 //#region lib/types/compression-limiter.js
 /** Instance-owned concurrency bound for native image transformations. */
@@ -469,7 +469,15 @@
 	try {
 		await ensureDurableDirectory(parent, await ensureDurableHome(dirname(dirname(resolve(root)))));
 		try {
-			await link(source, target);
+			try {
+				await link(source, target);
+			} catch (error) {
+				// hmdfs filesystem does not support hard links (EACCES/EPERM); publish via copy. COPYFILE_EXCL
+				// keeps EEXIST flowing into the outer catch, so a concurrent
+				// duplicate still goes through the sha256 integrity check.
+				if (!(error instanceof Error && (error.code === "EACCES" || error.code === "EPERM" || error.code === "ENOTSUP" || error.code === "ENOSYS"))) throw error;
+				await copyFile(source, target, constants.COPYFILE_EXCL);
+			}
 		} catch (error) {
 			/* v8 ignore next -- Private same-filesystem directories make EEXIST the only recoverable link race. */
 			if (!(error instanceof Error && "code" in error && error.code === "EEXIST")) throw error;
@@ -530,7 +538,15 @@
 	try {
 		await ensureDurableDirectory(parent, staged.boundary);
 		try {
-			await link(staged.path, target);
+			try {
+				await link(staged.path, target);
+			} catch (error) {
+				// hmdfs filesystem does not support hard links (EACCES/EPERM); publish via copy. COPYFILE_EXCL
+				// keeps EEXIST flowing into the outer catch, so a concurrent
+				// duplicate still goes through the sha256 integrity check.
+				if (!(error instanceof Error && (error.code === "EACCES" || error.code === "EPERM" || error.code === "ENOTSUP" || error.code === "ENOSYS"))) throw error;
+				await copyFile(staged.path, target, constants.COPYFILE_EXCL);
+			}
 		} catch (error) {
 			/* v8 ignore next -- Private same-filesystem directories make EEXIST the only recoverable link race. */
 			if (!(error instanceof Error && "code" in error && error.code === "EEXIST")) throw error;
